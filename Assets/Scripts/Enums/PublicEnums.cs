// TODO: Need to think where to put it
public enum BlockColor { Green, Blue, Red, MAX }
public enum BlockState { Idle, Selected, Moving, Disabled }
public enum BlockType { ColorBlock, Bomb, DestructibleObstacle, InvisibleObstacle }
public enum BombDirection{ Horizontal, Vertical }